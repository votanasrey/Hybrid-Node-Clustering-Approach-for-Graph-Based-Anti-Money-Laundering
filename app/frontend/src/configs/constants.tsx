export enum ECookies {
  AUTH_TOKEN = "@x:authToken",
  USER_ID = "@x:userId",
}
export enum EAppRoutes {
  DASHBOARD = "/",
  REGISTER = "/register",
  LOGIN = "/login",
}

export enum EContentTypes {
  axiosForJson = "application/json",
  axiosMutiplePart = "multipart/form-data",
}
